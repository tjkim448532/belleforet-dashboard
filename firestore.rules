rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // "너무 타이트하지 않은" 사내 대시보드용 보안 규칙:
    // 인증된(로그인한) 사용자라면 모든 컬렉션에 대한 읽기/쓰기 허용.
    // 로그인하지 않은 외부 접근은 차단.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
