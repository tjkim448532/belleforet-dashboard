# Belleforet Dashboard Agent Rules

## 🚫 [절대 원칙] 작업 폴더(Workspace) 외부 요청 즉시 거절 (Hard Boundary)

- **허용된 유일한 작업 영역**: `e:\앱\belleforet-dashboard` (프론트엔드 대시보드 프로젝트)
- **외부 영역 작업 요청 시 행동 지침**:
  - 본인의 지정된 프로젝트 폴더(`e:\앱\belleforet-dashboard`) 외의 폴더(예: `E:\앱\AWS\belleforet-data`, 백엔드 DB, AWS 인프라 등 타 프로젝트/외부 디렉터리)에 대한 분석, 파일 생성, 코드 수정, 스크립트 실행 요청이 들어올 경우, **작업을 시작하지 말고 즉시 거절**하십시오.
  - 외부 폴더를 우회하여 탐색하거나 실행하려는 어떠한 시도도 절대 금지합니다.
  - 거절 시 *"해당 요청은 현재 작업 폴더(belleforet-dashboard) 외부 영역에 대한 작업이므로 즉시 거절합니다."*라고 명확히 밝히고 즉시 종료하십시오.

---

## 📖 벨포레 데이터 통합 통제 API 바이블 (프론트엔드 무관용 원칙)

현재 백엔드의 V6 데이터 파이프라인 및 데이터 마트(`mat_v6_data_mart`) 연동이 0-Variance(오차 0원) 수준으로 완벽하게 구축되었습니다. 프론트엔드 팀은 대시보드 화면 구현 시 아래의 **'순수 소비자(Pure Consumer) 원칙'**을 어떠한 예외도 없이 엄수합니다.

### 1. 배열 직접 연산 전면 금지 (NO SLICE SUMMATION)
- 프론트엔드 단에서 배열 데이터(Array)를 `reduce`, `forEach`, `for` 문 등으로 직접 더해 총합(Grand Total)이나 소계(Subtotal)를 구하는 행위를 절대 금지합니다.
- 백엔드에서 소수점까지 완벽하게 정제된 정수형(Integer) 합계 데이터를 응답으로 내려줍니다. 프론트엔드는 내려온 값을 그대로 화면에 출력(Formatting)만 합니다. (단가, VAT 계산 등도 백엔드 몫입니다.)

### 2. 동적 화면 제어(Toggle) 시 가산 금지 및 감산(Minus) 원칙
- 칸반 보드 등에서 특정 팀/부서의 스위치를 끄는(Toggle Off) 경우, 남은 팀들의 숫자를 다시 '더해서' 총합을 만들지 마십시오.
- 반드시 백엔드가 처음 응답에 포함해 준 '전체 총합'에서 방금 끈 팀의 '소계(Subtotal)'를 '빼는(Minus)' 방식으로만 화면 숫자를 업데이트해야 합니다.

### 3. 장기 구간 조회 시 다중 호출 금지
- 다중 월(Multi-month)이나 긴 구간을 조회할 때, 일 단위 또는 월 단위 API를 프론트에서 반복(Loop) 호출하여 가산하지 마십시오.
- 반드시 API 파라미터 `startDate`와 `endDate`를 사용하여 단 1회의 API 호출로 백엔드에서 최종 집계된 완성본을 받아 사용해야 합니다.

### 4. 로컬 하드코딩 및 오타 텍스트 스마트 매핑 금지
- API가 내려주는 항목명(`category_name`, `team_name`, `shop_name` 등)에 오타나 예전 부서명이 섞여 있더라도, 프론트엔드에서 if문이나 정규표현식(Regex)으로 텍스트를 고쳐서(Mapping) 렌더링하지 마십시오.
- 오타 교정은 데이터 파이프라인(백엔드 Admin 매핑 화면)의 소관입니다. 데이터가 이상하면 임의로 숨기지 말고 그대로 노출시켜 운영팀이 Admin에서 교정하게 두어야 합니다.

### 5. 응답 규격 (100% camelCase) 및 표시 형식
- 모든 API의 응답 키워드는 `camelCase`로 정규화되어 반환됩니다. (DB 컬럼명인 snake_case 혼용 금지)
- 매출 데이터는 이미 DB 레벨에서 완벽히 소수점이 제거된 순수 정수(Integer)로 전달됩니다. 프론트에서는 별도의 `Math.round()` 처리를 하지 말고, `toLocaleString()` 등을 이용해 `#,##0` 형태의 천 단위 콤마(,) 서식만 입힙니다.

💡 **요약**: 프론트엔드는 백엔드가 조리해서 내어준 '완제품(JSON)'을 어떠한 가공이나 덧셈 없이 있는 그대로 접시(UI)에 예쁘게 담아내는 역할(Pure Consumer)에만 집중합니다. 데이터 조작(Data Manipulation) 코드가 프론트엔드 레포지토리에서 발견될 경우 즉각 롤백(Reject) 처리됩니다.

---

## 📜 V6 백엔드 API 개발 5대 철칙 (Backend Guardrails)

백엔드 팀에 새로운 API를 요청하거나 수정을 지시할 때 티켓(Jira, Notion, Slack) 상단에 강제하는 5대 절대 철칙입니다:

### 1. [Zero-Proxy] API 서버 내 자바스크립트 연산 100% 금지
- **요구사항**: Next.js 라우트 내부에서 `.reduce()`, `.map()` 등을 활용한 합산(SUM), 평균(AVG) 계산, 날짜 빼기 등의 비즈니스 로직을 절대 구현하지 마십시오.
- **통제 기준**: 모든 연산과 조건 분기(`CASE WHEN`)는 데이터베이스(MariaDB 프로시저/뷰)에서 완료되어야 합니다. API는 완성된 결과물(Flat Data)을 꺼내서 JSON 키값만 매핑해 프론트엔드에 전달(Passthrough)하는 '얇고 빠른 파이프' 역할만 수행해야 합니다.

### 2. [SSOT 수호] 골드 마트(Mart) 테이블 외 접근 엄금
- **요구사항**: 어떠한 경우에도 API 라우트 단에서 `raw_`(브론즈)나 `fact_`(실버) 테이블을 직접 `SELECT`하거나 `JOIN`하지 마십시오.
- **통제 기준**: 모든 API는 사전에 정제 완료된 `mat_v6_`(골드 마트) 테이블 또는 승인된 `dim_`(차원) 테이블만 바라봐야 합니다. 쿼리에 하위 테이블 접근이 발견되는 PR(Pull Request)은 즉시 반려됩니다.

### 3. [Pure Data] 매직 스트링(Magic String) 및 UI 종속성 주입 금지
- **요구사항**: 데이터 응답 JSON에 프론트엔드 렌더링을 위한 화면용 텍스트(예: `"[소계]"`, `"[총합계]"`)를 하드코딩해서 섞어 넣지 마십시오.
- **통제 기준**: DB의 NULL 값은 빈 문자열(`""`)로 치환하지 말고 순수하게 `null`로 내려주십시오. 상태 식별이 필요하다면 UI 텍스트가 아닌 명확한 논리값(`boolean`)이나 메타데이터(`subtotalLevel` 등)로 반환하여 프론트엔드가 자체적으로 화면을 그리도록 분리하십시오.

### 4. [ETL 격리] 외부 API 호출 및 데이터 적재 로직 금지
- **요구사항**: API 서버(Next.js) 내부에서 외부 날씨 API 등을 직접 찔러 데이터를 가져오거나 DB에 백필(Backfill)하는 작업을 수행하지 마십시오.
- **통제 기준**: 데이터 수집 및 적재는 AWS Lambda나 별도 Python 스크립트 기반의 ETL 전용 파이프라인에서 수행합니다. 백엔드 API는 오직 DB에 적재된 데이터를 '읽기(Read-only)'만 해야 합니다.

### 5. [DRY 원칙] 공통 유틸리티(모듈) 사용 강제
- **요구사항**: 날짜 파싱(`date_id` 변환), 에러 처리, 인증 등 반복되는 로직을 개별 API 라우트 파일마다 복사-붙여넣기(하드코딩) 하지 마십시오.
- **통제 기준**: 반드시 `src/lib/utils/` 등에 공통 모듈을 생성하고 이를 import하여 사용하십시오. 로직 변경 시 단 한 곳의 파일만 수정하면 전체 API에 반영되는 구조를 유지해야 합니다.
