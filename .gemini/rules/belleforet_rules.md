# 📖 벨포레 프론트엔드 전담 AI 핵심 협업 원칙 (Belleforet Global Agent Rules)

## 1. 🚨 [최상위 절대 무관용 원칙] 백엔드 코드 수정 및 AWS 인프라 조작 절대 금지 (NEVER TOUCH BACKEND / NO AWS CLI)
- **STRICT READ-ONLY BOUNDARY (백엔드 절대 불침범 원칙)**: 어떠한 상황, 어떠한 지시가 있더라도 백엔드 소스코드, API 라우트 핸들러, DB 스키마/DDL, 저장 프로시저(Stored Procedure), ETL 파이프라인 스크립트, AWS RDS/Lambda를 **절대로 직접 수정, 패치, 변형, 배포하지 마십시오.** 백엔드는 **100% 읽기 전용(Strict Read-Only)** 경계입니다.
- **NO AWS CLI / NO LAMBDA INVOKE (AWS CLI 및 람다 직접 호출 전면 금지)**: 프론트엔드 에이전트는 `aws lambda invoke`, `aws s3 cp/sync/rm`, `aws stepfunctions`, `aws rds`, `aws events` 등 **그 어떠한 AWS CLI 및 클라우드 백엔드 인프라 조작 명령어도 절대 실행해서는 안 됩니다.**
- **NO MANUAL S3 ERROR RE-INGESTION (S3 에러 폴더 임의 재적재 금지)**: S3 `quarantine/error/` 또는 `error/` 폴더에 있는 파일을 임의로 `csv/`로 복사하거나 재적재하려 하지 마십시오. 재시도 실패 잔재 파일의 임의 복사는 데이터 중복 및 0-Variance 파괴를 초래합니다.
- **NO BACKEND MUTATION**: 데이터 누락이나 오차 발생 시 백엔드 코드를 건드리지 않고, 오직 기존의 공식 V5/V6 API 엔드포인트와 정규 규격만을 소비하여 프론트엔드/관리자 단에서만 대응합니다.
- **REJECT BREAKING FRONTIER HACKS**: 백엔드 연산이 필요한 데이터 누락 건을 프론트엔드 단에서 `reduce`나 정규식 오타 추측 로직으로 임의 연산하지 않습니다. 백엔드 조치가 필요한 경우 `backend_api_request.md` 공식 명세서로 소통합니다.

## 2. 🛡️ [응답 및 협업 태도 원칙] 분석 우선 및 임의 코딩 금지
- **ANALYSIS & EXPLANATION FIRST**: 사용자가 "원인이 뭐지?", "왜 이렇게 나오지?" 등 원인 규명이나 현상 분석을 질문할 때는 임의로 프론트엔드/백엔드 코드를 먼저 수정하지 마십시오. 반드시 원천 데이터와 API 응답을 기반으로 팩트를 정밀 분석하여 명확한 설명과 답변을 먼저 제공해야 합니다.
- **NO ARBITRARY FRONTEND PATCH (스마트 치환 금지)**: 백엔드에서 과거 레거시 명칭이나 오매핑된 텍스트가 내려오더라도, 프론트엔드 코드에 `if`문이나 정규식으로 임의 치환(`replace`)하는 땜질식 코드를 절대 작성하지 마십시오. 원천 DB 정정을 위한 공식 요청서(`backend_api_request.md`)를 작성하여 백엔드 에이전트가 처리하도록 합니다.
- **ZERO ARBITRARY NUMBERS (거짓/추측 숫자 박멸)**: 진본 엑셀과 시스템 데이터 대사 시 임의의 추측값이나 가짜 폴백을 일체 배제하고, 1원 단위까지 전수 대사하여 명확한 근거(매핑 누락, 안분 룰 편차, 부가세 단수 등)를 투명하게 밝힙니다.

## 3. 백엔드 소통 및 명세서 공식 작성 (Backend API Request Document)
- API 수치 누락, 파라미터 누락, 신규 필드 필요 시 억지로 코드를 수정하지 않고, **`backend_api_request.md`** 아티팩트에 요청 번호(`REQ-V5-...`)와 함께 명확한 JSON 구조로 백엔드 조치 요청서를 정리합니다.

## 4. 📖 벨포레 데이터 통합 통제 API 공식 바이블 (The Bible v5.2) 엄수 (SSOT)
- **NO SLICE SUMMATION (프론트엔드 직접 합산 절대 금지)**: 프론트엔드는 배열 데이터를 `reduce`나 `for`문으로 직접 더해서 총합이나 소계를 구해서는 안 되며, 백엔드가 제공하는 완성된 `camelCase` 정규화 수치만 사용합니다.
- **다중 월/기간 조회 원칙**: 대시보드 기간 조회 시 API를 반복 호출하지 않고 `startDate`와 `endDate` 파라미터를 사용하여 1회 호출로 처리합니다.
- **NO FALLBACK CHAINS (Strict Typing)**: 프론트엔드 코드 내에서 스네이크 케이스 필드나 임시 필드들을 혼용하는 방어적 폴백 체인(예: `totalSales || todayActual || total_sales`)을 절대 사용하지 마십시오. 백엔드가 보장하는 단일 SSOT 키에만 완벽하게 1:1로 매핑합니다.
- **동적 화면 제어(Toggle Off) 시 마이너스 연산**: 특정 부문을 끌 때 백엔드 전체 총합에서 끈 부문의 백엔드 소계(`totalSales`)를 빼는 방식으로 구현합니다.
- **표준 정렬 순서 (Standard Sort Order)**: `콘도` ➔ `골프` ➔ `식음` ➔ `대관` ➔ `레져본부` ➔ `레져본부(외주)` ➔ `목장` ➔ `모토아레나` ➔ `주차관제` ➔ `기획전` ➔ `합계`

## 5. 🏛️ 공식 9대 부문(카테고리) 및 46개 표준영업장 매핑 기준 (The Definitive Bible v5.2 Mapping)
| 부문 (categoryCode / categoryName) | 소속 본부 (teamName) | 표준 영업장 (standard_name) | 비고 |
| :--- | :--- | :--- | :--- |
| **콘도** (`콘도`) | **리조트사업본부** | 객실, 객실 서비스 | 객실료 + 부대수익 |
| **골프** (`골프`) | **골프사업본부** | 골프, 클럽-레스토랑, 클럽-스타트하우스, 프로샵 | 그린피/카트피/식음/임대용품 |
| **식음** (`식음`) | **콘텐츠기획본부** | 남도예담, 딜라이트, 밤밤테이블, 밤밤트럭, 벼루재촌, 브리스킷346, 쿠치나, 썸머랜드(식음), BHC(멕시카나), CU편의점, 투썸플레이스 | 직영 식음 + 임대 식음 매장 |
| **대관** (`대관`) | **세일즈본부** | 대관, 벨포레홀 | MICE, 연회 및 단체 행사 |
| **레져본부** (`레져본부`) | **레저본부** | 썸머랜드, 원더풀, 사계절썰매장, 마운틴카트, 익스트림 루지, 마리나 클럽, 미디어아트센터, 미디어-뮤지엄카페, 미디어-기프트샵, 벨포레 목장, 벨포레 목장(체험), 디노 시네마 | 직영 어트랙션 & 전시 & 목장 |
| **레져본부(외주)** (`레져본부(외주)`) | **레저본부** | 놀이동산 | 놀이기구 + 미니골프 통합 |
| **목장** (`목장`) | **레저본부** | 얼룩말카페 | 목장 특화 카페 |
| **모토아레나** (`모토아레나`) | **모토아레나** | 모토아레나, 모토아레나 렌탈샵, 핏스탑 | 서킷 주행/렌탈/스낵 |
| **주차관제** (`주차관제`) | **주차관제** | 주차관제 | 주차 무인 정산기 |
| **기획전** (`기획전`) | **콘텐츠기획본부** | 기획전 | 기획 패키지 상품 |

## 6. 🏷️ [원천 데이터 명칭 불변 원칙] 백엔드 제공 명칭 임의 창작/가공 절대 금지 (STRICT AS-IS RENDERING)
- **NO ARBITRARY NAME CREATION (임의 명칭 창작/치환 절대 금지)**: 프론트엔드는 백엔드 API 및 원천 DB가 전달하는 부문명(`categoryName`), 본부/팀명(`teamName`), 파트명(`partName`), 영업장명(`facilityName`), 채널명(`channelName`) 등의 모든 명칭 텍스트를 1글자도 임의로 수정, 유추, 매핑, 치환하거나 새로운 이름을 만들어내서는 **절대 안 됩니다.**
- **AS-IS 1:1 RENDERING (100% 원본 수신 표출)**: 백엔드가 응답으로 내려준 텍스트 그대로 화면에 1:1로 렌더링해야 합니다. 프론트엔드가 자체적인 매핑 테이블이나 if문으로 이름을 덮어씌우는 하드코딩 행위를 영구히 금지하며, 오직 백엔드가 전달한 원본 명칭만을 완벽한 SSOT로 취급합니다.

## 7. 🎯 경영 목표 역추산 및 매출비중 대입 절대 원칙 (Target Reverse-Engineering)
- **월별 실측 비중 다이렉트 대입**: 미래 매출 계획 수립 시 연간 고정 비율을 쓰지 않고, **해당 월의 실측 매출 비중 곡선을 직접 곱하여 대입**한다 (`/api/v5/report/business-plan?date=YYYY-MM-DD&growthRate=...`).
- **진행 중(MTD) 데이터 착시 분리**: 8월 등 미마감 월은 전년 31일치와 단순 합산하여 평균을 왜곡하지 않으며, **공식 마감월(1~7월 등)과 진행 중(MTD) 뱃지를 명확히 분리**하여 표출한다.

## 8. 🛡️ 파이프라인 무결성 및 0-Variance 검증 원칙 (Pipeline Integrity & Zero-Variance SOP)
- **가상 숫자 및 수동 UPDATE 전면 금지**: 엑셀 벤치마크 합계에 맞추기 위해 DB 테이블을 수동으로 조작하거나 임의 보정 레코드를 끼워 넣는 행위를 엄격히 금지하며, 오직 원천 데이터(Raw) ➔ ETL 저장 프로시저(SP) ➔ Mart View의 순수 연산 쿼리 교정 및 Re-run을 통해서만 0-Variance를 달성한다.
- **골프 회계 계정 분리 원칙**: `raw_골프_정산_v6`의 `그린피, 카트피, 기타매출, 렌탈피`는 이미 **공급가액(순매출 Net)**이다. 부가세(`VAT`) 및 선수금/외상(`Guest Ledger`)이 별도 행으로 존재하므로, 전체 행을 단순 합산하거나 `/ 1.1`을 중복 적용해서는 안 된다.
- **레저/모토 온라인 티켓 정산 Net/Gross 분리**: 현장 POS(`raw_포스_메뉴_v6`) 발권분과 온라인 티켓(`raw_티켓_판매_v6`) 발권분을 정가 기준으로 결합 적재하여 대행사 수수료 사전 차감으로 인한 매출 과소 계상을 원천 차단한다.
- **2025년 7월 전사 실적 골든 벤치마크 SSOT (`25년7월한달.xlsx`)**:
  - **전사 순매출 (Net Sales):** `₩ 2,388,741,295 원`
  - **전사 부가세 (VAT):** `₩ 238,736,977 원`
  - **전사 총매출 (Gross Sales):** `₩ 2,627,478,272 원`
  - **총 판매 수량:** `78,747 건`
  - **부서별 실적:** 객실 `536,957,968 원`, 골프 `760,862,685 원`(골프식음 제외), 식음 `547,420,918 원`, 레저 `288,336,112 원`, 모토 `106,457,588 원`, 대관 `36,897,639 원`, 주차 `4,358,184 원`.

## 9. 100% 무결점 검증 & 원스톱 운영 배포
- 코드 수정 후에는 항상 사전 `npm run build`를 실행하여 0 에러/0 경고를 확인합니다.
- 작업 완료 후 **Firebase Hosting 배포 (`firebase deploy --only hosting`)** 및 **Git 커밋/푸시 (`git commit & git push`)**까지 한 호흡으로 깔끔하게 마칩니다.

## 10. 🚫 [최상위 데이터 규율] 프론트엔드 수치 연산 절대 금지 및 100% As-Is 바인딩 (ZERO FRONTEND CALCULATION)
- **ZERO FRONTEND ARITHMETIC (0% 프론트엔드 연산)**: 프론트엔드 코드는 합산(`reduce`), 나눗셈(`/`), 가중치 곱셈(`*`), 보정 계수 산출 등 **그 어떠한 수치 연산도 직접 수행해서는 안 됩니다.**
- **AS-IS NUMERICAL BINDING (백엔드 수치 100% 직결)**: 1인당 평균 그린피, 객단가(ADR), 객실당 매출(RevPAR), 점유율(Occ), 달성률, 증감률 등 모든 파생/평균 지표는 **오직 백엔드가 사전 정산하여 내려준 완성된 JSON 필드만을 100% 그대로 바인딩(Display-Only)**합니다.
- **NO CLIENT-SIDE DATA HACKS (프론트엔드 임의 보정 금지)**: 백엔드 API 간에 숫자가 불일치하거나, 내장객 수가 중복 집계되거나, 단가가 왜곡되어 내려오더라도, **프론트엔드에서 임의로 나누거나 곱해서 끼워 맞추는 '클라이언트 단 땜질 보정'을 영구히 금지**합니다.
- **ONLY BACKEND REQUEST FOR NUMERICAL DEFECTS (백엔드 요청서 단일 창구)**: 수치 왜곡이나 모수 불일치 발견 시, 프론트엔드는 임의 가공 없이 현상을 정확히 파악하여 **오직 `backend_api_request.md` 공식 조치 요청서에 등록하고 백엔드 DB 쿼리/ETL 단에서 근본적으로 수정**하도록 통제합니다.

## 11. 🏛️ 화면·지표별 3대 데이터 트랙 (Track 1/2/3) 표준 연동 기준 (v6.0-Track-Guide)
- **트랙 3 [UNIFIED] (통합 마트 - 경영진 대시보드 SSOT)**: `mat_v6_data_mart_revenue (track_type='UNIFIED')`
  - 현장 POS + 온라인 티켓 완전 통합 (엑셀 일일마감보고서와 99.7% 일치).
  - 적용 대상: **대시보드 메인 전체** (`revenue-summary`, `matrix-weekly`, `sales-hierarchy`, `pie-chart`, `business-plan`, `customer-journey-bundles`).
  - 백엔드 API 기본값이므로 프론트엔드 코드 수정 불필요.
- **트랙 1 [LEDGER] (회계 원장 - 재무/세무 결산용)**: `mat_v6_data_mart_revenue (track_type='LEDGER')`
  - POS & ERP 마감 원장 순수 원본 (온라인 티켓 미포함 순수 현장 결산).
  - 적용 대상: 관리자 감사/대사 화면 (`/admin/reconciliation`, `/admin/audit-status`).
  - 쿼리 파라미터: `?track=LEDGER`.
- **트랙 2 [TICKET] (발권 분석 - 온라인/패키지용)**: `fact_ticket_sales_v6`
  - 다올 / OTA 온라인 티켓 원천 분석 (`top-ticket-items`).


